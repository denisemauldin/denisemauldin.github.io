<%= form_for(@role) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :lead %><br>
    <%= f.text_field :lead %>
  </div>
  <div class="field">
    <%= f.label :training %><br>
    <%= f.select :training, options_for_select(@trainings.collect { |s| [s[0].humanize, s[0]] }, selected: @role.training) %>
  </div>
  <div class="field">
    <%= f.label :shadowing %><br>
    <%= f.text_field :shadowing %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
